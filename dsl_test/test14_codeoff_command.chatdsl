# Test Case 14: Codeoff Command
# Description: Test disabling code-only mode.

set test_dir = "./dsl_test"

# Enable code-only mode first
/codeonly

# Now disable it
/codeoff

# Test that regular text works (would need LLM to respond normally)
chat --> Tell me a story about a dragon.
